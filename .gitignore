node_modules
# dist (commented out - needed for production deployment with SSR)
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables (보안 중요!)
.env
.env.local
.env.*.local

# Claude settings (may contain credentials)
.claude/migrations/

# Toss mTLS certificates (NEVER commit these!)
/certs/
*.crt
*.key
*.pem
*.ait
insiderpulse.ait
